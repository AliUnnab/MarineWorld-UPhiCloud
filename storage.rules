rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read and authenticated/anonymous write access for all company assets
    match /{allPaths=**} {
      allow read: if true;
      allow write: if true;
    }
  }
}
