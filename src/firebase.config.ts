/**
 * Firebase Client SDK Configuration & Core Service Instances
 */
export {
  app,
  auth,
  db,
  storage,
  analytics,
  googleAuthProvider,
  firebaseConfig,
  default,
} from "./lib/firebase";
